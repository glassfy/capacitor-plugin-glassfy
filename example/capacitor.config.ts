import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.example.app',
  appName: 'example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
